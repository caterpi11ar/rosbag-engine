{
  "name": "rosbag-engine",
  "version": "0.1.0",
  "private": true,
  "description": "Browser-based ROSBag parsing, playback, and rendering engine",
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=8.0.0"
  },
  "scripts": {
    "build": "pnpm -r run build",
    "dev": "pnpm -r run dev",
    "test": "pnpm -r run test",
    "lint": "eslint --fix --cache ."
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "@antfu/eslint-config": "^4.13.2",
    "@types/node": "^22.15.29",
    "eslint": "^9.28.0",
    "typescript": "^5.8.3",
    "vitest": "^0.33.0"
  }
}
